
spring.application.name=car-rent
spring.profiles.active=native
spring.cloud.config.server.native.searchLocations=classpath:/config
spring.cloud.config.uri=http://localhost:7771

spring.cloud.config.name=db-config
#这里使用本机数据库，如果使用远程数据库，则将local换成dev
spring.cloud.config.profile=${config.profile:zsl}
n.instance_id:${random.value}}
eureka.instance.preferIpAdrress=true
eureka.instance.instanceId=${spring.application.name}:${spring.applicatio